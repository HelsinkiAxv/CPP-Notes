#include <iostream>
using namespace std;

class Solution {
public:
    vector<vector<string>> partition(string s) {
        int center = 0;
        for (int i = center; i < (2*n) - 1; ++center) {
            int left = center / 2;
            int right = left + (right % 2);
            if (left == right) {
                partition.push_back(s[i]);
            }
            else {
                string ans = " ";
                for (int i = )
            }
        }
    }
};